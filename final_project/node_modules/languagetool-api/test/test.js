var languagetool = require("../index.js");

var params = {
  language: "pl-PL",
	text: "lekasz",
	enabledCategories: ["TYPOS", "CASING"],
	enabledOnly: true
};

languagetool.check(params, function(err, res){
	if(err){
	   console.log(err);
	} else{
	   console.log(res);
	};
});